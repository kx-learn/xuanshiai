## Goal
继续执行 `.pi/plan/ai-backend-production-closeout-followup-20260926.md`，完成当前可验证范围内的 AI/语音后端安全、可靠性、接口契约和文档收口：修复语音音频访问测试结构，核对并在必要处完善隐私修订、撤权、注销/重新激活和本地 TTS 访问边界，确保所有语音音频签发调用点行为一致，并同步语音协议文档。生产 AI 保持关闭，不以不可用的真实依赖或 Provider 环境替代验收。

## Acceptance criteria
- `tests/test_ai_voice_audio_access.py` 的私有 URL 缺失/过期/篡改签名测试与跨用户回放测试均为完整、可收集的独立测试；相关测试文件通过 Python 编译和 Ruff 检查。
- 私有本地语音路径仅覆盖 `tts/` 与 `voice/tts/`；签名绑定路径、过期时间、用户 ID 和 `privacy_revision`；外部 Provider URL 不被站内签名改写；ASR 不产生可匿名下载的本地文件。
- 本地受保护存储对未签名、篡改、跨用户、旧 revision、过期 URL 返回 `403`，数据库状态不可确认返回 `503`，合法且存在的文件返回 `200`，合法签名但文件已清理返回 `404`；撤权、注销和重新激活不会使历史 URL 意外恢复访问。
- 所有 REST TTS、普通语音 WebSocket、墨相师 WebSocket 的本地 TTS 返回值均经过当前用户和隐私修订绑定；签名拒绝不会触发整段 TTS 回退；外部音频 URL保持原样。对应定向测试覆盖并通过。
- AI consent 的 grant/revoke 隐私修订递增和同事务 outbox/清理语义经代码核对；注销批准的非活跃账号状态和重新激活后的历史音频访问行为经代码与回归测试核对，若发现缺口则补齐最小修复。
- 以下本地验证通过，或对现有失败明确区分归因：
  - 语音音频访问、普通语音 WS、Aliyun Provider 定向测试；
  - AI/语音定向测试集合；
  - 相关 app/tests 文件 Ruff、Python 编译和 `git diff --check`。
- `docs/api/语音.md` 删除重复播放说明、重复 `§5` 标题和旧未签名 `/storage/voice/tts/` 示例，统一说明 `user`、`revision`、`expires`、`signature`、撤权/注销/数据库故障/文件清理行为及 `AI_POLICY_DENIED`、`AI_TEMPORARILY_UNAVAILABLE` WebSocket 处理。
- `docs/api/墨相师实时整理WebSocket.md` 明确服务端绑定的 `user`/`revision` 参数、撤权/注销/旧 revision/数据库故障返回行为，并保持当前 `moxiang_journey` 协议；不把 WebSocket 写入 OpenAPI HTTP `paths`。
- 生产 AI 配置和默认关闭行为不被开启或削弱；不使用 fake、静默 skip、源码断言或伪造 evidence 代替真实 MySQL/Redis/Provider 验收。若 Docker、真实数据库、Redis 或 Provider 沙箱仍不可用，最终报告准确的阻塞命令和未完成范围。

## Boundaries
- 只修改 AI、语音、隐私访问控制及其直接依赖、相关测试和对应 API/协议文档；不扩展到支付、普通媒体、腾讯直播或无关重构。
- 保留工作区现有全部用户修改；不执行 `git reset --hard`、`git checkout --`、覆盖或清理来源不明的改动，不提交或推送代码。
- 不启用生产 AI，不填充或泄露真实密钥、数据库密码、Provider 凭据，不连接或修改生产数据。
- 不把 WebSocket 协议混入 OpenAPI HTTP 路径，不通过放宽校验、吞异常或删除有效测试来“通过”检查。
- 真实 MySQL/Redis、Provider 沙箱、Worker、删除传播、retention、监控告警、灰度和 rollback 只有在依赖实际可用时验证；不可用时保留为明确阻塞，不声称生产发布已批准。