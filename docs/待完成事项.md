# 未完成事项记录

> 按 PROJECT_RULES §九：逐项标注状态、阻塞原因和下一步；后续任务完成后只将对应事项标记为「已完成」并保留原记录，不得直接删除。

## 一、AI 功能生产启用门禁（Provider / 合规 / 保留期）

| 事项 | 状态 | 阻塞原因 | 下一步 |
| --- | --- | --- | --- |
| `ai_policy_approved` 合规批准 | 未批准 | 需产品和合规书面批准授权范围、Provider 区域/训练策略、保留期、撤回/删除/导出能力（统一方案 §13.4 第 1-2 项） | 走合规评审流程后置 `AI_POLICY_APPROVED=true` |
| `ai_provider_approved` Provider 批准 | 未批准 | Provider 数据处理、DPA/数据出境、训练用途审查未完成（统一方案 §13.4 第 3 项） | Provider 审查通过后置 `AI_PROVIDER_APPROVED=true`，生产必须使用非 mock Provider |
| `ai_retention_policy_version` 保留期策略 | 未配置 | 保留期策略版本尚未冻结 | 冻结策略后配置 `AI_RETENTION_POLICY_VERSION` |
| 生产启用 AI | 保持 `AI_FEATURE_DISABLED` | 三个批准项 + 非 mock Provider 任一缺失即校验失败、fail-closed | 全部满足后再逐个模块开闸（§13.2 逐阶段发布） |
| 回滚演练证据 | 未记录 | `artifacts/ai-rollback-drill.json` 尚未生成 | 完成一键关闭演练后写入证据，供 `scripts/verify_ai_release.py` 聚合 |
| 生产 AI 开关 | 全部关闭 | 未批准 | 保持 `AI_MASTER_ENABLED=false` 等全部关闭 |

## 二、Phase 4/5 启动条件（P-04 / S-06 / C-06 / 学习排序）

> 以下仅记录启动条件，本期不实现。只有全部满足才可另立计划启用；未获批前所有 AI 开关保持关闭。

### P-04 语音/ASR（Phase 4）

- 产品和合规书面批准语音/转写用途、语言、地域、原始音频与 transcript 保留期、导出和删除 API。
- Provider 完成 DPA/数据出境/训练用途审查，且可以按 task/user 证明删除。
- 音频上传有格式、时长、病毒、内容治理和访问控制；失败可稳定回退文字输入，不重复扣费。
- 未满足前不提供任何音频路由（`docs/api/AI画像.md` §16）。

> **开发档实现状态（2026-08-21，ADR `voice-provider-baseline` + `voice-conversation-realtime`）：**
> 后端 voice 服务域已就绪（`app/services/voice/`：Protocol + Mock/Aliyun provider + Gateway + transcribe handler + 实时对话编排器 + 阿里云流式 ASR client），路由 `POST /voice/transcribe`（异步任务）、`POST /voice/synthesize`（同步）、`WS /voice/conversation`（实时半双工对话）已注册。`ai_voice_enabled` / `ai_voice_conversation_enabled` 默认关闭、`ai_voice_provider` 默认 mock，生产门禁 fail-closed 已扩展。
>
> **生产启用前遗留项：**
> 1. 阿里云 NLS SDK 实际接入（`_AliyunVoiceClient.recognize_audio` 当前为 NotImplementedError，仅 mock 可用；实时 ASR WS client 已有完整实现但 HMAC-SHA1 签名为简化版）。
> 2. 音频文件存储接入（`voice.py` 路由的 audio_ref 写入标注 TODO）。
> 3. ~~临时音频过期清理任务实现~~（已完成 2026-09-09：`app/services/voice/cleanup.py` 统一清理函数 + `ai_worker._run_forever` 按 `ai_voice_audio_cleanup_interval_seconds` 节流定时清理，音频 retention `ai_voice_audio_retention_hours` 与 transcript retention 分开配置）。
> 4. 阿里云 DPA / 数据出境审查 + 产品/合规书面批准。
> 5. 前端 PCM 格式真机验证微信小程序 `onFrameRecorded` 兼容性。

### S-06 语义召回（Phase 4）

- embedding 只用于已确认且允许检索的字段，索引有 `projection_version`、`privacy_revision` 和撤回即时过滤。
- MySQL 可见性和硬条件仍是主门禁；模型输出不能成为 SQL/列名/`ORDER BY`/表名。
- 向量存储、索引与 ANN 检索组件须单独评审（延迟、成本、删除传播、保留期），未批准前 `ai_search_enabled` 保持关闭。
- 旧 `user_feature_vector.interest_vector` 不作为 embedding 使用；一期不接 ANN。

### C-06 兼容度质量评估（Phase 4）

- 先做人工标注、离线回放、覆盖/安全/公平性/成本/延迟报告，不自动改变规则权重。
- 新兼容度只写 shadow（`display_eligible=false`、`experiment_bucket=shadow`），不影响推荐排序与用户动作。

### Phase 5 学习排序

- 行为事件字典、明确用途授权、去标识化训练集、人工标注、校准、群体公平性评估、独立合规审查、在线监控和一键回滚全部就绪。
- 训练标签至少区分查看、喜欢、申请、接受、聊天、举报和拉黑，不把一次点击、会员购买或关系结果承诺混成单一标签。
- 学习排序永远运行在 `CandidateVisibilityService`、硬条件和账号状态之后。

## 三、消费循环调度与 Worker 部署

| 事项 | 状态 | 说明 |
| --- | --- | --- |
| Worker 启动方式 | 已完成 | `python -m app.workers.ai_worker`（详见 `docs/DEVELOPMENT.md` §10.2）；`--once --dry-run` 只读空转 |
| 任务恢复 | 已完成 | 过期租约由 reaper 回收转 `retry_wait`，心跳续租保护进行中 handler |
| 消费循环（`derivation_outbox` 删除传播/投影失效） | 已实现，需部署说明 | 由后台消费者执行（Task 4/8/9）；生产部署时需独立调度消费者进程 |
| 监控告警 | 部分完成 | `emit_ai_metric` 覆盖 queue_age/lease_reclaimed/retry_rate/schema_invalid/provider_429/provider_5xx/stale_rate/fallback_rate/deletion_propagation_seconds/outbox_backlog/purge_backlog；生产时序出口待接（当前结构化日志 + 进程内快照） |
| 审计写入 | 已完成 | `record_generation_audit` 写 `ai_generation_audit` 最小可追责信息，不写原文/原始 Provider response；写失败不阻塞业务 |

## AI 一期基础底座（Task 1，2026-08-11）

| 事项 | 状态 | 阻塞原因 / 证据 | 下一步 |
| --- | --- | --- | --- |
| 真实 MySQL/Redis/双 Worker 测试环境 | 已完成 | `compose.ai-test.yml` 已建立；MySQL 8、Redis 7、worker-a、worker-b 可启动；Redis 跨连接和双进程竞争测试通过 | 保持测试卷与业务开发库隔离 |
| AI schema P0 合约 | 已完成 | Task 2 迁移、幂等 bootstrap 与真实 MySQL schema 测试通过；补齐 turn/result/projection/session 生命周期字段 | 继续保留迁移 checksum 和回滚验收 |
| 画像会话历史唯一约束 | 已完成 | active session 改为 generated `active_slot` 唯一约束；多个已关闭 session 的真实测试通过 | 生产旧数据迁移前做一次备份演练 |
| Worker 短事务、取消与幂等 | 已完成 | Task 3 真实双 Worker 竞争、取消、租约恢复、心跳与 live revision/consent 复查测试通过 | 补一次真实进程被杀后的 reaper 演练 |
| AI consent 公共 API | 已完成 | GET/PUT/DELETE、scope/version/privacy revision、幂等重放、冲突、撤回清理任务均有路由与真实 MySQL 测试 | 上线前由产品/合规确认三类 consent 文案版本 |
| Gateway 审计与 Worker 指标 | 已完成 | Gateway 审计写入真实 `ai_generation_audit`；队列年龄、租约回收、重试、Provider 错误、删除传播等指标路径已接入 | 生产时序/告警出口仍需部署配置 |
| bootstrap 外键检查告警 | 已完成 | `REFERENTIAL_CONSTRAINTS` 检查已改用 `CONSTRAINT_SCHEMA`；真实 bootstrap 重新运行无该告警 | 保持 bootstrap 日志作为部署验收证据 |

### 一期收口后的仍需人工验收

- `AI_POLICY_APPROVED`、`AI_PROVIDER_APPROVED`、保留策略版本和真实 Provider 审批仍未完成；生产开关继续保持关闭。
- `artifacts/ai-rollback-drill.json` 尚未生成，需要完成一次关闭/回滚演练后补证据。
- 审计与指标目前已进入结构化日志和进程内快照，生产时序库、告警规则及独立 outbox/purge 消费者仍需部署确认。

## 四、跨任务遗留（各 task review 记录，非本任务引入）

- M-1（Task 8）：publish revision_no TOCTOU 属 carry-over，与本任务读取路径互补。**已修复（2026-08-15）**：`insert_immutable_profile_revision` 捕获 IntegrityError + SAVEPOINT + 3 次重试重读 MAX+1。
- M-4（Task 8）：search result stale 只按 target 方向标记未覆盖 viewer 属 carry-over；每次读取重新 decide 补充。**已修复（2026-08-15）**：`delete_ai_profile`、`delete_ai_profile_field`、`_invalidate_revoked_scope`、`_mark_derived_results_stale` 四处全部补上 JOIN `ai_search_snapshot` 覆盖 viewer 方向。
- 导出能力：在合规批准后单独启用，不作为首期默认路径。

## 五、G5 证据链脚手架（Task 10，2026-08-17 证据治理分支）

> 本轮在 `codex/ai-g5-g7-20260817` 分支补齐证据链结构与测试脚手架。本轮硬约束：禁止运行 pytest/ruff/python 脚本来验证，禁止构建/容器/微信/稳定性观察。所有"已完成"仅指结构/代码已写，运行验证 NOT_RUN。

| 事项 | 状态 | 阻塞原因 / 证据 | 下一步 |
| --- | --- | --- | --- |
| `artifacts/schemas/ai-evidence-v1.schema.json` | 已完成（结构） | G0-G7 gates、commandEvidence、hashMap、reviewer、result enum 完整 | schema 本身无需运行验证 |
| `scripts/build_ai_evidence.py` | 已完成（结构） | command allowlist、git state、redact、load_command_evidence、validate_evidence_shape、build_evidence、CLI 完整 | 待解除运行禁令后跑 `tests/test_build_ai_evidence.py` 驱动红/绿 |
| `tests/test_build_ai_evidence.py`（Task10 Step1） | 已完成（TDD 失败测试，写但不跑） | 覆盖 required fields、反例（空 JSON/旧 SHA/未知命令/非零 exit/result≠PASS）、validate_evidence_shape、redact、production+mock blocker、PENDING review blocker | 解除运行禁令后跑 `pytest tests/test_build_ai_evidence.py -q` 验证 |
| `scripts/verify_ai_release.py` `--target` 改造（Task10 Step3） | 已完成（结构） | 加 `--target internal|production`（required）、`--environment` deprecated 兼容期、evidence bundle 校验（schema/SHA/hash/时效/gates/production approvals） | 待跑 `python scripts/verify_ai_release.py --target internal` 验证 NO-GO |
| `tests/test_ai_release_gates.py` 更新 | 已完成（结构） | 补 `--target` required、production 强制、internal 拒绝 production、缺证据 bundle NO-GO、production 恒 NO-GO 断言 | 解除运行禁令后跑回归 |
| 质量集扩充（Task10 Step4） | 已完成（结构占位） | `ai-profile-quality.json`、`ai-search-quality.json`、`ai-compatibility-shadow-quality.json` 替换为带 `provenance`/`use_limitation`/`reviewed`/`allow_expansion` 的版本化结构；compatibility 保持 `allow_expansion=false`；metrics 全部 null（NOT_RUN） | 真实评测数据待 Task 11 后填入 |
| Ruff 修复（Task10 Step5） | 部分完成 | `derivation_outbox.py` F841×2 已修（519/528 行 `as exc` 未直接引用 exc，改为去掉绑定）；`profile.py` F402×3 与 `test_ai_fault_injection.py` F401×1 未修 | F402 疑为模块名与标准库 `profile` 同名告警，需跑 ruff 确认确切位置；F401 未找到未用 import，需跑 ruff 确认。硬约束禁止跑 ruff，留 TODO |
| migration rollback 计划（Task10 Step6） | 已完成（文档） | 新建 `docs/ai/AI_MIGRATION_ROLLBACK.md`（快照引用、DDL 步骤、迁移矩阵、故障补偿、down 丢列损失范围）；未执行真实 migration | 待 Task 11 容器重建授权后在 disposable DB 填充 |
| `docs/DEVELOPMENT.md` 更新 | 已完成 | 追加 G5 证据链脚手架状态说明 | 见下方 §六 |
| `docs/待完成事项.md` 更新 | 已完成 | 本节登记 | 持续维护 |
| Ruff 全量检查 | NOT_RUN | 硬约束禁止跑 ruff | 解除运行禁令后跑 `ruff check app tests scripts` |
| `tests/test_build_ai_evidence.py` 运行 | NOT_RUN | 硬约束禁止跑 pytest | 解除运行禁令后跑 |
| `tests/test_ai_release_gates.py` 运行 | NOT_RUN | 硬约束禁止跑 pytest | 解除运行禁令后跑 |
| `verify_ai_release.py --target internal` 运行 | NOT_RUN | 硬约束禁止跑 python 脚本验证 | 解除运行禁令后跑，预期 NO-GO |

## 六、G7 稳定性/回滚占位（Task 13，2026-08-17 证据治理分支）

> 本轮只生成结构占位 artifact，不执行 3-5 天稳定性观察，不执行回滚演练。production 恒 NO-GO。

| 事项 | 状态 | 阻塞原因 / 证据 | 下一步 |
| --- | --- | --- | --- |
| `artifacts/ai-stability-report.json` | 已完成（NOT_RUN 占位） | 按 `ai-stability-report.template.json` 结构生成；`result: NOT_RUN`；blockers 列出"3-5天观察未开始"等 | 待 Task 11 后做真实 3-5 天观察 |
| `artifacts/ai-rollback-drill.json` | 已完成（NOT_RUN 占位） | 按 `ai-rollback-drill.template.json` 结构生成；`result: NOT_RUN`；6 步 rollback 全 NOT_RUN | 待 Task 11 Step7 回滚演练后填入 |
| 3-5 天稳定性观察 | NOT_RUN | 本轮不观察 | 解除运行禁令后执行 |
| 回滚演练 | NOT_RUN | 本轮不演练 | 待 Task 11 Step7 |
| Graphify 更新 | 已完成 | 2026-08-31 在工作区根执行 `graphify update .`，增量 AST 重提取并刷新 `graphify-out/graph.json`；SQL 节点仍因缺少 `tree_sitter_sql` 依赖而跳过 | 后续代码/文档变更继续执行增量更新；需要 SQL 图谱时再单独安装可选依赖 |

## 七、production 结论

- production target 在本计划内默认 NO-GO；需另行产品/合规/成本/运营/监控/回滚批准，且仍不包含生产 Provider/ASR。
- `verify_ai_release.py --target production` 当前必须 exit 2 + `disabled-until-approved`，绝不误报 GO。
- 任何证据缺失 → exit 2 + `AI_FEATURE_DISABLED`。

## 八、墨相师双画像建构（2026-08-31）

| 事项 | 状态 | 阻塞原因 / 证据 | 下一步 |
| --- | --- | --- | --- |
| `personal` / `ideal_partner` 双主体 master 会话 | 已实现并完成定向测试 | WebSocket 切换、主体隔离、旧会话 stale 兼容、异步任务主体标记与重连复用测试通过；相关 AI 集合排除 4 个既有失败后为 415 passed、1 skipped | 联调时继续核对日志中的 subject、session_id 与 task_id，不用页面标签代替后端证据 |
| 本地后端服务 | 已启动并通过健康检查 | `127.0.0.1:8000/api/v1/health` 与当前 WLAN 地址 `10.5.163.243:8000/api/v1/health` 均返回 200 | 网络切换后重新确认 WLAN IPv4，并同步小程序开发地址后再编译 |
| 真实 Provider 双阶段输出 | 待人工联调 | 自动测试覆盖协议与 Mock/受控 Provider 行为，但不能证明生产 Provider 对第三方观察边界、明确偏好提炼和双主体输出始终合规 | 使用获批 Provider 和测试账号完成个人阶段、愿遇阶段、具体对象追问、分别发布与断线重连验收；保留脱敏日志 |
| AI 测试集合中的 4 个既有失败 | 待单独治理（非本功能引入） | `test_voice_conversation.py` 3 项存在旧文本/回复预期偏差；`test_ai_tasks.py::test_start_task_rejects_expired_lease` 使用固定 2026-08-07 时间且当前未触发预期异常 | 单独建立基线修复任务，先确认测试意图和时间注入方式，不在双画像变更中混修 |

## 九、墨相师四阶段复验收口（2026-09-01）

| 事项 | 当前状态 | 证据 / 后续 |
| --- | --- | --- |
| 原有 5 个后端失败 | 已修复 | 租约过期校验、语音旧接口兼容与固定日期测试已修复；后端全量为 `1006 passed, 19 skipped, 1 xfailed` |
| AI migration 目标隔离 | 已修复 | `--target test` 强制使用显式 `AI_TEST_DATABASE_URL`，不再回落到开发库；独立 MySQL 测试库校验到当前版本 |
| Linux + Redis 双 Worker 竞争 | 已通过 | Docker 中两个独立 Worker 进程的竞争领取与真实 Redis 测试为 `2 passed`；Worker 镜像已补齐测试和 Provider 依赖 |
| 发布到画像投影链路 | 已通过 | 真实 MySQL 集成测试验证 publish 后生产 handler 将 projection 置为 `active`，不是测试手工造数 |
| 阿里云 Token / ASR / TTS | 已通过一次真实联调 | 修正 REST 网关域名后，使用当前环境配置运行 live 测试为 `3 passed`；密钥内容未写入文档 |
| 微信开发者工具人工闭环 | 阻塞 | 用户已登录且路由烟测通过；但 `api/config.uts` 仍指向不可达旧地址，真实请求超时，需切换开发 API 后补做登录态、双主体切换、确认、分别发布、断线重连、Console 与 Network 检查 |
| production 发布 | NO-GO | 仍需生产审批、监控、回滚演练、合法域名与微信端人工证据；本次结果只支持本地开发和自动化链路 |

## 十、统一旅程六维画像与下游读取（2026-09-02）

| 事项 | 当前状态 | 证据 / 阻塞原因 | 后续动作 |
| --- | --- | --- | --- |
| `moxiang_journey` 六维候选抽取 | 已实现（统一 AI Gateway） | 每个已落库 turn 通过 `AIGateway.structured_extract(session_kind="master")` 得到 typed fields/entries/patches，再由服务端六维白名单映射写入 `ai_profile_candidate`；任务 payload 不含原文；`tests/test_moxiang_master_prompt.py` 与 `tests/test_deepseek_provider.py` 锁定 master 的 allowlist fields + patches，`tests/integration/ai/test_moxiang_journey_worker_real_db.py` 在隔离 MySQL 中验证真实 Worker 落 candidate、完成任务且不越级写 draft | 获批真实 Provider 联调时继续核对自由表达、第三方观察和主体隔离 |
| 候选 → 邀请 → draft → confirm → publish | 已实现 | 接受邀请才提升为 `ai_profile_draft_field(suggested)`；隔离 MySQL 的真实 Worker 已验证候选、邀请接受、字段确认、发布，并落成 `personal_searchable` / `personal_compatibility` 两类 active 投影 | 用测试账号在微信开发者工具完成一次端到端人工操作并保留脱敏日志 |
| 已发布画像 → 搜索/匹配投影 | 已实现（后端接口就绪） | `personal_searchable` / `personal_compatibility` 只接受 confirmed + published + consent_active；撤回/删除会失效投影与派生结果 | 当前页面暂不主动调用 `/ai/search-drafts` 和 `/ai/compatibility/*`，后续按产品接入排期 |
| 墨相师历史恢复 | 已修复 | 前端解包 `{ success, data }`，`answer_text` 兼容映射为 `content`，失败响应不覆盖本地消息；前端契约测试通过 | 真实微信端断线重连仍需人工验收 |

## 十一、纸飞机资料解锁（2026-09-03）

| 事项 | 状态 | 阻塞原因 / 证据 | 后续动作 |
| --- | --- | --- | --- |
| 服务端目标资料解锁与 80 积分原子扣减 | 已完成（代码与单测） | `paper_plane_profile_unlock` 迁移、viewer+target 唯一约束、幂等键重放/冲突、可见性与拉黑门禁已实现；聚焦 pytest `4 passed` | 真实 MySQL 迁移与微信端联调时补充并发和端到端证据 |
| 联系方式交换申请 | 未完成 | 本接口不返回手机号/微信号；独立纸飞机交换申请接口由前端任务接入 | 完成双方确认的交换申请 API 后再联调 |

## 十二、墨相师提示词离线优化（2026-09-04）

| 事项 | 状态 | 证据 / 限制 | 后续动作 |
| --- | --- | --- | --- |
| 30 轮基线驱动的 master 抽取提示词修订 | 已完成（离线契约） | 基于 `run-2026-09-04T094123Z` 已完成任务中的漏抽/错桶样本，补充最小事实拆分、多维覆盖、情绪表达/长期愿景/个人空间优先级和软证据保留规则；未改变主体隔离、Schema 或阈值 | 保留当前基线产物，下一次真实回放使用新 prompt version 后比较 diff |
| 真实 Dots Provider 复评分 | NOT_RUN（按本轮要求） | 本轮不重新调用真实 Provider；原快照采集时 30 个抽取任务中 15 succeeded、13 queued、1 running、1 retry_wait，因此 `recall=0.5312` 不能单独归因于提示词 | 后续复评前先等待 Worker 队列排空，再采集终态快照；不得把 dedup 通过当作任务全部完成 |


## 兴趣标签 personal-v2 联调验收（2026-09-10）

状态：目录已细分为17区268项，原216项全部保留；自定义标签已接入，76项定向后端回归与小程序编译通过，部署联调与端侧视觉验收待完成。

- 新增统一 personal_tags 字段，旧存储列保留；上线先后端后前端，不批量迁移生产数据。
- 自定义兴趣标签每项2～10字符、最多3项，与系统标签共用10项总上限；后端格式校验、内容审核及 `tags.custom` 显式存储标记已接通。
- 本地小程序已由 HBuilderX 编译成功；开发主包3419.3KiB超2MiB门禁，微信自动化仍无法取得可核验页面元素，尚未完成四尺寸交互验收。增量详情见前端 docs/个人标签扩充实施报告.md。
- 下一步：部署后用测试账号验证兴趣标签保存/清空、旧标签整理、关系期待回显及公开隐私；按前端 `docs/个人标签与择偶标准整理方案.md` 验收。不把本地隔离测试当作已部署结果。
